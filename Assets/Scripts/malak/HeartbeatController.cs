using UnityEngine;

public class HeartbeatController : MonoBehaviour
{
    //public AudioSource heartbeatSource;
    //public Transform police; // assign police chara here
    //public float maxDistance = 15f;

    //void Update()
    //{
    //    float distance = Vector3.Distance(transform.position, police.position);

    //    if (distance < maxDistance)
    //    {
    //        if (!heartbeatSource.isPlaying)
    //            heartbeatSource.Play();

    //        // increase speed when closer
    //        heartbeatSource.pitch = 1 + (maxDistance - distance) / maxDistance;
    //    }
    //    else
    //    {
    //        heartbeatSource.Stop();
    //    }
    }
    //below code for testing (will be removed)
    //public AudioSource heartbeatSource;

    //void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.H))
    //        StartHeartbeat();

    //    if (Input.GetKeyDown(KeyCode.J))
    //        StopHeartbeat();
    //}
    //void Start()
    //{
    //    heartbeatSource.Stop();
    //}

    //public void StartHeartbeat()
    //{
    //    if (!heartbeatSource.isPlaying)
    //        heartbeatSource.Play();
    //}

    //public void StopHeartbeat()
    //{
    //    heartbeatSource.Stop();
    //}

//}
