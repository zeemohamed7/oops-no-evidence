/// <summary>
/// Attach this to any floor GameObject so the mop can clean footprints on it.
/// Also attach it to the blood floor (alongside BloodPool) so blood cleaning works there too.
/// No configuration needed — it is a marker only.
/// </summary>
public class CleanableFloor : UnityEngine.MonoBehaviour { }
